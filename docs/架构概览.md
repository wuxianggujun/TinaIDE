# TinaIDE 架构概览

> 最后人工核验：2026-09-09

TinaIDE 当前采用 `app + core/* + feature/* + tools/*` 的模块化结构，依赖注入基于 Koin。
和旧文档最大的区别仍然是：**默认编译 / LSP 链路已经切到 native toolchain + Android sysroot，PRoot 退回为可选 Linux 环境。**

## 分层总览

```text
┌─────────────────────────────────────────────────────────────┐
│ UI Layer                                                   │
│ MainPortalActivity / MainActivity / Compose screens        │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│ App & Feature Layer                                         │
│ app + feature:editor / help / terminal / settings / viewer  │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│ Core Layer                                                  │
│ compile / ndk / lsp / editor-lsp / plugin / project ...     │
└──────────┬───────────────┬────────────────┬─────────────────┘
           │               │                │
┌──────────▼─────────┐ ┌───▼──────────────┐ ┌▼──────────────────┐
│ Native Runtime     │ │ Optional Linux   │ │ Optional X11      │
│ tina-toolchain     │ │ Runtime          │ │ Desktop (:x11)    │
│ Android sysroot    │ │ PRoot + Ubuntu   │ │ linux-desktop     │
│ native clangd/run  │ │ guest toolchain  │ │ libXlorie.so      │
│ 主进程             │ │ terminal / tools │ │ 未在真机验证      │
└────────────────────┘ └──────────────────┘ └───────────────────┘
```

## 当前主业务模块结构

### App 壳

- `:app`
  启动流程、主页导航、项目编辑器入口、跨模块装配。

### Feature 层

- `:feature:editor`
- `:feature:help`
- `:feature:output`
- `:feature:packages`
- `:feature:projectlist`
- `:feature:settings`
- `:feature:terminal`
- `:feature:tutorial`
- `:feature:viewer`
- `:feature:wizard`
- `:feature:workspace`

### Core 层

- `:core:apk-builder`
- `:core:cmake`
- `:core:common`
- `:core:compile`
- `:core:config`
- `:core:crash`
- `:core:database`
- `:core:debug`
- `:core:designsystem`
- `:core:editor-lsp`
- `:core:editor-view`
- `:core:git`
- `:core:i18n`
- `:core:logging`
- `:core:linux-desktop`
- `:core:linux-distro`
- `:core:lsp`
- `:core:model`
- `:core:ndk`
- `:core:network`
- `:core:packages`
- `:core:plugin`
- `:core:project`
- `:core:proot`
- `:core:search`
- `:core:security`
- `:core:storage`
- `:core:text-engine`
- `:core:tree-sitter`

### 外部本地依赖

- `external/tina-android-tree-sitter`
- `external/rikkahub`
- `external/termux-proot`
- `external/termux-terminal`
- `external/termux-x11`
- `external/android-rsync`
- `external/tina-exec`
- `external/xcrash`
- `external/immersionbar`
- `external/devicecompat`
- `external/xxpermissions`

### 附加本地模块 / included modules

除了主业务模块外，`settings.gradle.kts` 还显式包含了若干本地模块：

- `:termux-terminal:terminal-emulator`
- `:termux-terminal:terminal-view`
- `:tina-exec:runtime`
- `:tina-exec:integration`
- `:xcrash`
- `:immersionbar-local`
- `:immersionbar-ktx-local`
- `:devicecompat-local`
- `:xxpermissions-local`
- `:tools:template-common`
- `:tools:template-native-activity`
- `:tools:template-sdl3`
- `:tools:template-terminal`
- `:termux-x11-lorie`（`projectDir` 指向 `external/termux-x11/lorie`）
- `:termux-x11-shell-loader-stub`（`external/termux-x11/shell-loader/stub`，仅 compileOnly 桩）

`termux-x11` 使用扁平路径而不是 `:termux-x11:lorie`：后者会隐式创建 `:termux-x11`
父项目并执行上游根 `build.gradle`，其自带 repository 声明与本仓库的
`FAIL_ON_PROJECT_REPOS` 冲突。

## 关键边界

### 入口结构

当前 App 入口已经分成两层：

- `MainPortalActivity`：主页 / 项目入口 / 设置跳转
- `MainActivity`：打开项目后的主编辑器工作区

不要再把“首页”和“项目编辑器”理解成同一个 Activity。

### 默认编译链路

基础编译、运行、clangd/LSP 依赖的是：

- `AndroidNativeToolchainManager`
- `AndroidSysrootManager`
- `CompileProjectUseCase`
- `NativeClangdConnectionProvider`

也就是说，默认开发链路不要求 PRoot 先安装完成。

### 编辑器语言服务分流

`LspEditorManager` 当前会按文件类型和配置做四类分流：

1. C / C++：走 `LspClientSession`，后端可选 native clangd、PRoot clangd、remote LSP
2. CMake：走 `CMakeLanguageServiceSession`
3. Make / `.mk`：走 `MakeLanguageServiceSession`
4. 其他语言：若存在匹配插件，则走 `LspPluginManager + PluginLspConnectionProvider`

这意味着“编辑器语言服务”已经不是 `clangd` 单一路径。

### RikkaHub 接入

TinaIDE 已移除自研聊天、渠道、会话仓储和工具调用系统。RikkaHub 源码以
`external/rikkahub` included build 形式纳入仓库，并通过 `rikkahub-embedded`
library 打包进 TinaIDE 主 APK。编辑器侧边栏通过 `DrawerContent` 嵌入
`RikkaHubEmbeddedChatPane`，设置页通过 `SettingsActivity` 嵌入
`RikkaHubEmbeddedSettingsPane`。

RikkaHub 的模型、渠道、聊天、MCP 和相关设置界面由 RikkaHub 自身维护；TinaIDE 只负责打包、启动入口和宿主生命周期边界。默认本地验证使用 `:rikkahub:embedded:compileDebugKotlin`，不要求连接真机。当前 Gradle 9.6 + AGP 9.2.1 组合下，Problems report 可能保留 AGP 内部 `VariantDependenciesBuilder.createTestComponents` 触发的 project dependency notation 弃用告警；该告警不表示 RikkaHub 源码编译失败。

### 可选 Linux 环境

`core:proot` 负责的是可选 Linux 环境：

- 自研 Linux 发行版安装
- PRoot 生命周期
- Linux 终端与 guest toolchain

它主要服务：

- Linux shell / 命令行场景
- 某些依赖 Linux 侧工具的插件 / 扩展能力
- 部分调试链路

当前唯一支持的发行版是 Ubuntu 24.04。Alpine 支持已在 `0.18.29` 整体移除，
`AlpineMirrorManager`、Alpine 镜像设置项和 `ConfigKeys.AlpineMirrorUrl` 都已删除。

### 可选 X11 图形桌面（尚未在真机验证）

`core:linux-desktop` 承载 Linux 图形桌面链路，当前状态是代码路径完整但
**没有在真机跑通 XFCE 桌面**，不要按“已交付功能”对待。

进程边界是这一层最重要的约束：

- X server 与渲染 UI 都在 `:x11` 进程，由 `core/linux-desktop/src/main/AndroidManifest.xml` 声明
- 原因是 lorie 的 `cmdentrypoint.cpp` 把 libc 的 `exit()` / `abort()` 覆盖成 `_exit()`，
  X server 线程执行 `exit(dix_main(...))`，任何一次 `FatalError` 都会不可捕获地终止所在进程；
  放在主进程会让 X server 崩溃连带杀死 IDE
- 主进程负责“算出要跑什么”（`LinuxDesktopHostProcessPlanner` 组装 host 命令行），
  `:x11` 只负责“跑”；guest 会话由 `:x11` spawn，避免在 X server 进程里初始化 Koin
- `$TMPDIR` 是 host 与 guest 的唯一交汇点，socket 布局契约见 `X11SocketLayout`

渲染与输入直接复用 termux-x11 的 `MainActivity` / `LorieView`。`MainActivity` 不能移除：
native 侧 `activity.cpp` 用 `FindClassOrDie("com/termux/x11/MainActivity")` 并按名字读
`LorieView.activity` 字段，类名和字段类型都写死在 `.so` 里。库 manifest 里属于“独立 app”
的部分（LAUNCHER 入口、导出的偏好接收器、`KeyInterceptor`）已在模块 manifest 中挡掉。

关键类：`LinuxDesktopService` / `LinuxDesktopServiceImpl`（状态机与环境变量导出）、
`X11ServerLauncher` / `X11ServerService`（前台服务宿主）、`UbuntuLinuxDesktopCoordinator`（串联）。
详细说明见 [`core/linux-desktop/README.md`](../core/linux-desktop/README.md)。

### 启动安装流程

首次启动时，`StartupFlowManager` 默认会拉起依赖安装页，并且：

- 默认只安装 `Android sysroot + native toolchain`
- 不自动安装 Linux 发行版 / PRoot

只有显式进入 Linux 环境入口时，`DependencyInstallViewModel` 才会追加：

- Linux distro runtime metadata
- `Linux rootfs`
- `PRoot guest toolchain`

## 关键源码落点

### 启动与导航

- `app/src/main/java/com/wuxianggujun/tinaide/ui/MainPortalActivity.kt`
- `app/src/main/java/com/wuxianggujun/tinaide/MainActivity.kt`
- `app/src/main/java/com/wuxianggujun/tinaide/startup/StartupFlowManager.kt`

### 依赖安装

- `feature/workspace/src/main/java/com/wuxianggujun/tinaide/ui/workspace/DependencyInstallActivity.kt`
- `feature/workspace/src/main/java/com/wuxianggujun/tinaide/ui/workspace/DependencyInstallViewModel.kt`

### Native 编译与运行

- `core/compile/src/main/java/com/wuxianggujun/tinaide/core/compile/CompileProjectUseCase.kt`
- `core/compile/src/main/java/com/wuxianggujun/tinaide/core/compile/strategy/SingleFileStrategy.kt`
- `core/compile/src/main/java/com/wuxianggujun/tinaide/core/compile/strategy/CMakeStrategy.kt`
- `core/compile/src/main/java/com/wuxianggujun/tinaide/core/compile/NativeMakeBuildStrategy.kt`
- `core/ndk/src/main/java/com/wuxianggujun/tinaide/core/ndk/AndroidNativeToolchainManager.kt`
- `core/ndk/src/main/java/com/wuxianggujun/tinaide/core/ndk/AndroidSysrootManager.kt`

### 编辑器与语言服务

- `feature/editor/src/main/java/com/wuxianggujun/tinaide/editor/EditorManager.kt`
- `feature/editor/src/main/java/com/wuxianggujun/tinaide/editor/session/DocumentSession.kt`
- `core/editor-lsp/src/main/java/com/wuxianggujun/tinaide/core/editorlsp/LspEditorManager.kt`
- `core/editor-lsp/src/main/java/com/wuxianggujun/tinaide/core/editorlsp/BuiltinLanguageServiceSession.kt`
- `core/editor-lsp/src/main/java/com/wuxianggujun/tinaide/core/editorlsp/CMakeLanguageServiceSession.kt`
- `core/editor-lsp/src/main/java/com/wuxianggujun/tinaide/core/editorlsp/MakeLanguageServiceSession.kt`
- `core/editor-view/...`
- `core/text-engine/...`

### LSP 与插件

- `core/lsp/src/main/java/com/wuxianggujun/tinaide/core/lsp/NativeClangdConnectionProvider.kt`
- `core/lsp/src/main/java/com/wuxianggujun/tinaide/core/lsp/PRootClangdConnectionProvider.kt`
- `core/lsp/src/main/java/com/wuxianggujun/tinaide/core/lsp/RemoteLspConnectionProvider.kt`
- `core/lsp/src/main/java/com/wuxianggujun/tinaide/core/lsp/LspClientSession.kt`
- `core/plugin/src/main/java/com/wuxianggujun/tinaide/plugin/lsp/LspPluginManager.kt`
- `core/plugin/src/main/java/com/wuxianggujun/tinaide/plugin/lsp/PluginLspConnectionProvider.kt`

### Linux / PRoot

- `core/proot/src/main/java/com/wuxianggujun/tinaide/core/proot/PRootBootstrap.kt`
- `core/proot/src/main/java/com/wuxianggujun/tinaide/core/proot/SelfHostedLinuxDistroRuntime.kt`
- `core/proot/src/main/java/com/wuxianggujun/tinaide/core/proot/RootfsDistroRuntime.kt`

### X11 图形桌面

- `core/linux-desktop/src/main/AndroidManifest.xml`（`:x11` 进程边界）
- `core/linux-desktop/src/main/java/com/wuxianggujun/tinaide/core/linuxdesktop/LinuxDesktopServiceImpl.kt`
- `core/linux-desktop/src/main/java/com/wuxianggujun/tinaide/core/linuxdesktop/X11ServerLauncher.kt`
- `core/linux-desktop/src/main/java/com/wuxianggujun/tinaide/core/linuxdesktop/X11SocketLayout.kt`
- `core/linux-desktop/src/main/java/com/wuxianggujun/tinaide/core/linuxdesktop/UbuntuLinuxDesktopCoordinator.kt`
- `core/linux-desktop/README.md`

### RikkaHub / 帮助 / 插件

- `app/src/main/java/com/wuxianggujun/tinaide/ui/compose/components/DrawerContent.kt`
- `app/src/main/java/com/wuxianggujun/tinaide/settings/SettingsActivity.kt`
- `external/rikkahub/embedded`
- `feature/help/src/main/java/com/wuxianggujun/tinaide/core/help/HelpRepository.kt`
- `core/plugin/src/main/java/com/wuxianggujun/tinaide/plugin/PluginManager.kt`

## 核心流程

### 1. 首次启动

1. `MainPortalActivity` 启动前调用 `StartupFlowManager.checkStartupFlow()`
2. 检查 `WorkspaceSetupCompleted`
3. 检查 `Android sysroot` 与 `native toolchain` 是否就绪
4. 未就绪时进入 `DependencyInstallActivity`
5. `DependencyInstallViewModel` 安装并校验运行资产

### 2. 打开项目

1. 用户在 `MainPortalActivity` 选择项目
2. `IProjectSession` 打开项目并注册路径
3. 跳转到 `MainActivity`
4. 编辑器、文件树、终端、底部面板开始装配

### 3. 编译 / 运行

1. UI 触发编译动作
2. `CompileProjectUseCase` 识别项目类型与运行配置
3. 对应 `BuildStrategy` 调用 native toolchain 或 PRoot 侧工具
4. `ProgramRunner` 负责运行产物或准备终端命令

### 4. 编辑器语言服务

1. 打开文件后，`LspEditorManager` 判断文件类型与当前配置
2. C/C++ 走 `LspClientSession`，按设置选择 native / PRoot / remote
3. CMake / Make 走内建语言服务会话
4. 插件语言走 `LspPluginManager`
5. 诊断、补全、跳转、语义 token 同步到底部面板和编辑器状态

### 5. 可选 Linux 环境安装

1. 用户在设置页或工作区入口显式发起安装
2. PRootBootstrap 调用 SelfHostedLinuxDistroRuntime 安装发行版 rootfs
3. `DependencyInstallViewModel` 继续安装 guest toolchain、sysroot、native toolchain
4. Linux 终端与相关功能可用

## 当前最容易误解的点

- PRoot 不是默认编译器宿主环境。
- “首次启动安装 rootfs” 已经不是默认事实；默认安装的是 sysroot 与 native toolchain。
- 编辑器语言服务不是只有 clangd；CMake / Make 已有内建语言服务，其他语言还可以走插件 LSP。
- Linux rootfs 来源由 `tools/linux-distro` manifest 描述，不再通过旧静态 Docker rootfs 资产包注入。
- Linux 发行版只剩 Ubuntu 24.04，Alpine 相关代码和设置项已在 `0.18.29` 删除。
- X11 桌面代码路径完整，但尚未在真机验证；不要按已交付功能写文档或排期。
- 进程不只有主进程：`:x11`、`:sdl`、`:sdl2`、`:gui`、`:crash` 各有边界，
  初始化逻辑不能混用（见 `TinaApplication` 的多进程分流）。

## 许可证边界

TinaIDE 整体以 GPL-3.0-or-later 分发，起因是集成 termux-x11 的 X server。
涉及新增第三方依赖时，必须先确认其许可证与 GPL-3.0 兼容。

当前存在一个未解决的阻塞项：`external/rikkahub` 的附加限制违反 GPL-3.0 第 7 条，
**包含它的构建产物在冲突解决前不得对外分发**。清单与处理路径见 [`NOTICE.md`](../NOTICE.md)。

## 进一步阅读

- [快速开始](快速开始.md)
- [开发指南](开发指南.md)
- [LSP 调试指南](guides/LSP-Debug-Guide.md)
- [X11 图形桌面模块说明](../core/linux-desktop/README.md)
- [PRoot 运行时重构设计](design/PRoot-Runtime-Refactor.md)
- [PRoot 功能分析](design/PRoot-Feature-Analysis.md)
