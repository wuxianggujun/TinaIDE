# Linux 发行版远程清单与镜像回落：实施记录

> 状态：已完成
>
> 最后人工核验：2026-07-10
>
> 当前事实源：[自研 Linux 发行版运行时](docs/linux-distro-self-hosted-runtime.md)

## 已落地能力

- `:core:linux-distro` 的 manifest 已支持清单级 `mirrors` 前缀替换规则。
- `LinuxDistroInstaller` 会先尝试 artifact 官方 URL，再按规则派生并去重镜像候选。
- `:core:proot` 已提供 `RemoteLinuxDistroManifestSource` 和 `LinuxDistroCatalogRepository`。
- 远程加载顺序为：6 小时内的新鲜缓存 → Registry 多端点 → 过期缓存 → 内置 asset。
- 启动和普通发行版列表使用内置或缓存入口，不隐式访问网络；远程刷新必须显式调用。
- Registry 路径为 `linux-distro/manifest.v1.json`，内置 manifest 仍是离线兜底。

## 模块边界

- `:core:linux-distro`：数据模型、镜像候选、下载、校验、解包，保持为可独立测试的底层能力。
- `:core:proot`：网络、缓存、Registry 端点与 Android Context 装配。
- TinaIDE 默认编译链路仍是 native tina-toolchain + Android sysroot；Linux distro/PRoot 是可选环境。

## 当前不做

- 不增加镜像优先级开关或自定义 rootfs URL 管理界面。
- 不让 `:core:linux-distro` 依赖 `:core:network`。
- 不在启动阶段自动刷新远程 manifest。
- 不把本实施记录继续当作路线图；后续事实变化直接更新专题文档和 `CHANGELOG.md`。

## 回归入口

```bash
./gradlew :core:linux-distro:testDebugUnitTest --console=plain
./gradlew :core:proot:testDebugUnitTest --console=plain
./gradlew :app:compileArm64DebugKotlin --console=plain
```

重点覆盖：manifest 镜像解析、官方源失败后的镜像回落、取消不重试、新鲜/过期缓存、远程失败回落和启动路径不联网。
